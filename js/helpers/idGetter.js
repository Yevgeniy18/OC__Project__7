 const getTagValue = (value) => {
	
	// return  new RemoveTags().removeTagFromList({value:value})

	let selectedTags = document.getElementsByClassName('selected-tag')
	for (let tag of selectedTags){
		console.log(tag.children)
	
	
	}
}
