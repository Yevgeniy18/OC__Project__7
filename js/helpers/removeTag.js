function RemoveTag( value ){
    let tagSection = document.getElementById('selected-tags')
    let tags = tagSection.getElementsByClassName('selected-tag')
    for (let tag of tags){
        if (tag.children[0].textContent === value){
    
        }
    }


}